<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jimmy.rxandroid">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--简单示例-->
        <activity android:name=".ui.JavaSimpleActivity"/>

        <!--更多示例-->
        <activity android:name=".ui.MoreActivity"/>

        <!--Lambda示例-->
        <activity android:name=".ui.RxAndroidActivity"/>

        <!--网络请求-->
        <activity android:name=".ui.NetworkActivity"/>

        <!--安全示例-详细内容-->
        <activity android:name=".ui.SafeActivity"/>

    </application>

</manifest>